'use client'; import { AnimatePresence,motion } from 'framer-motion'; import { X } from 'lucide-react';
export default function Modal({open,onClose,image,alt}){return <AnimatePresence>{open&&<motion.div className="modal" role="dialog" aria-modal="true" aria-label="Image preview" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={onClose}><button className="modal-close" onClick={onClose} aria-label="Close image"><X/></button><motion.img initial={{scale:.94}} animate={{scale:1}} src={image} alt={alt||''} onClick={e=>e.stopPropagation()}/></motion.div>}</AnimatePresence>}
