'use client'; import { useEffect,useState } from 'react'; import Link from 'next/link'; import { Menu,X } from 'lucide-react'; import { navLinks } from '@/lib/constants'; import MobileMenu from './MobileMenu'; import { siteConfig } from '@/config/siteConfig';
export default function Navbar(){const [scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>24);f();addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return <><header className={`nav ${scrolled?'scrolled':''}`}><Link className="brand" href="/" aria-label={siteConfig.gym.name}>{siteConfig.gym.displayName.split(' ')[0]}<span>{siteConfig.gym.displayName.split(' ')[1]}</span><b>{siteConfig.gym.descriptor}</b></Link><nav>{navLinks.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</nav><Link className="join-mini" href="/membership">Join now</Link><button className="menu-toggle" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></header><MobileMenu open={open} onClose={()=>setOpen(false)} /></>}
