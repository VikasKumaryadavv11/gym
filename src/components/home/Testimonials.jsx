'use client'; import { useState } from 'react'; import { ChevronLeft,ChevronRight,Star } from 'lucide-react'; import { testimonials } from '@/data/testimonials'; import SectionTitle from '@/components/ui/SectionTitle';
export default function Testimonials(){const [index,setIndex]=useState(0);const t=testimonials[index];return <section className="testimonial-section"><div><SectionTitle eyebrow="MEMBER STORIES" title={<>The work<br/>speaks for <em>itself.</em></>}/><div className="testimonial-controls"><button aria-label="Previous testimonial" onClick={()=>setIndex((index+2)%3)}><ChevronLeft/></button><button aria-label="Next testimonial" onClick={()=>setIndex((index+1)%3)}><ChevronRight/></button></div></div><article className="quote"><div>{[...Array(t.rating)].map((_,i)=><Star key={i} size={17} fill="currentColor"/> )}</div><blockquote>“{t.text}”</blockquote><p><strong>{t.name}</strong> — {t.detail}</p></article></section>}
