'use client'; import Image from 'next/image'; import Link from 'next/link'; import { motion } from 'framer-motion'; import { programs } from '@/data/programs'; import SectionTitle from '@/components/ui/SectionTitle';
export default function Programs(){return <section className="section dark-section"><SectionTitle eyebrow="TRAINING DISCIPLINES" title={<>Train your way.<br/><em>Push further.</em></>} copy="Every format is built around measured progression, not random sweat."/><div className="program-grid">{programs.map((p,i)=>{const Icon=p.icon;return <motion.article initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.08}} className="program" key={p.title}><Image src={p.image} fill sizes="(max-width:600px) 100vw, 50vw" alt={p.title}/><div className="program-overlay"/><div className="program-copy"><span><Icon size={20}/> {p.duration}</span><h3>{p.title}</h3><p>{p.description}</p><Link href="/programs">Explore program <b>→</b></Link></div></motion.article>})}</div></section>}
