import Button from '@/components/ui/Button'; import { Check } from 'lucide-react'; import { memberships } from '@/data/membership'; import SectionTitle from '@/components/ui/SectionTitle';
export default function MembershipPlans(){return <section className="section plans-section"><SectionTitle center eyebrow="MEMBERSHIP" title={<>Choose your <em>level.</em></>} copy="No hidden fees. Pause or change your membership whenever life changes."/><div className="plans">{memberships.map(p=><article className={'plan '+(p.featured?'featured':'')} key={p.name}>{p.featured&&<b className="recommended">MOST POPULAR</b>}<h3>{p.name}</h3><p>{p.description}</p><div className="price"><sup>₹</sup>{p.price}<span>/ month</span></div><ul>{p.features.map(f=><li key={f}><Check size={16}/>{f}</li>)}</ul><Button href={'/membership?plan='+p.name} variant={p.featured?'primary':'outline'}>Choose {p.name}</Button></article>)}</div></section>}
